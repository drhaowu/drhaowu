---
// 统一的Hero组件 - Liquid Glass风格
---

<section class="hero-section">
  <div class="hero-content">
    <div class="hero-text">
      <h1 class="hero-title">Dr. Hao Wu</h1>
      <p class="hero-subtitle">Digital Geographer & Media Creator</p>
      <p class="hero-description">
        PhD in Digital Geography from UCL, specializing in the intersection of digital technologies and queer spaces. 
        Passionate about teaching, research, and creative media production.
      </p>
      <div class="hero-buttons">
        <a href="#academic" class="btn btn--glass">Academic Profile</a>
        <a href="#media" class="btn btn--primary">Media Work</a>
      </div>
    </div>
    <div class="hero-avatar">
      <img src="/assets/images/avatar.png" alt="Dr. Hao Wu" class="hero-avatar-img">
    </div>
  </div>
</section>

<style>
  .hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 0;
    position: relative;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 4rem;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero-text {
    max-width: 600px;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    color: #ffffff;
    text-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #078FFB, #42DEE4);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    font-weight: 600;
    color: #ffffff;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    margin-bottom: 1.5rem;
  }

  .hero-description {
    font-size: 1.1rem;
    line-height: 1.7;
    color: #ffffff;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
    margin-bottom: 2rem;
    opacity: 0.9;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-avatar {
    width: 300px;
    height: 300px;
    background: #ffffff;
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    border: 4px solid rgba(255, 255, 255, 0.3);
    position: relative;
    overflow: hidden;
  }

  .hero-avatar::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transform: rotate(45deg);
    animation: shimmer 3s infinite;
  }

  .hero-avatar-img {
    width: 90%;
    height: 90%;
    border-radius: 16px;
    object-fit: cover;
    position: relative;
    z-index: 1;
  }

  @keyframes shimmer {
    0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
    100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
  }

  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
      text-align: center;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.25rem;
    }

    .hero-avatar {
      width: 250px;
      height: 250px;
      margin: 0 auto;
    }

    .hero-buttons {
      justify-content: center;
    }
  }
</style>