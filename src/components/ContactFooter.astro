<footer class="portfolio-footer" id="contact">
	<div>
		<p class="portfolio-footer__eyebrow">The End</p>
		<h2>Thank you for reviewing<br /><em>my portfolio.</em></h2>
	</div>
	<div class="portfolio-footer__contact">
		<p>Contact</p>
		<a class="portfolio-footer__email" href="mailto:ucfawuc@gmail.com">ucfawuc@gmail.com</a>
		<div class="portfolio-footer__socials" aria-label="Social links">
			<a href="https://twitter.com/CasperWU6" target="_blank" rel="noreferrer">
				<span>Twitter</span>
				<span>@CasperWU6 <span aria-hidden="true">↗</span></span>
			</a>
			<a href="https://www.linkedin.com/in/hao-wu-digital-geographer/" target="_blank" rel="noreferrer">
				<span>LinkedIn</span>
				<span>hao-wu-digital-geographer <span aria-hidden="true">↗</span></span>
			</a>
		</div>
	</div>
	<div class="portfolio-footer__meta">
		<span>Dr Hao Wu · Digital Geographer / Media Creator</span>
		<span>© {new Date().getFullYear()} Dr Hao Wu</span>
	</div>
</footer>

<style>
	.portfolio-footer {
		position: relative;
		z-index: 1;
		display: flex;
		flex-direction: column;
		align-items: stretch;
		gap: clamp(2.5rem, 6vw, 5rem);
		width: min(100%, 1440px);
		min-height: 25rem;
		margin: 0 auto;
		padding: clamp(4rem, 8vw, 8rem) clamp(1.25rem, 7vw, 8.5rem) clamp(2rem, 4vw, 4rem);
	}

	.portfolio-footer__eyebrow {
		margin: 0 0 1rem;
		font-family: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
		font-size: 0.72rem;
		letter-spacing: 0.12em;
		text-transform: uppercase;
	}

	.portfolio-footer h2 {
		margin: 0;
		font-size: clamp(2.8rem, 5.5vw, 6.5rem);
		font-weight: 700;
		letter-spacing: -0.07em;
		line-height: 0.94;
	}

	.portfolio-footer h2 em {
		color: #ff9a00;
		font-style: normal;
	}

	.portfolio-footer__contact {
		width: min(100%, 42rem);
		color: #344054;
	}

	.portfolio-footer__contact p {
		margin: 0 0 0.4rem;
		color: #ff9a00;
		font-size: 0.82rem;
		font-weight: 700;
		text-transform: uppercase;
	}

	.portfolio-footer__email {
		color: inherit;
		font-size: clamp(1rem, 1.8vw, 1.6rem);
		text-decoration: none;
	}

	.portfolio-footer__socials {
		display: grid;
		gap: 0.55rem;
		margin-top: 1.8rem;
	}

	.portfolio-footer__socials a {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		padding: 0.7rem 0;
		border-top: 1px solid rgba(23, 23, 23, 0.14);
		color: #344054;
		font-family: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
		font-size: 0.68rem;
		letter-spacing: 0.02em;
		text-decoration: none;
		transition: color 180ms ease, border-color 180ms ease;
	}

	.portfolio-footer__socials a > span:last-child {
		text-align: right;
	}

	.portfolio-footer__socials a > span:last-child span {
		margin-left: 0.25rem;
		font-size: 0.9rem;
	}

	.portfolio-footer__socials a:hover,
	.portfolio-footer__socials a:focus-visible {
		border-color: #ff9a00;
		color: #171717;
	}

	.portfolio-footer__meta {
		display: flex;
		width: 100%;
		margin-top: auto;
		justify-content: space-between;
		gap: 1rem;
		padding-top: 1rem;
		border-top: 1px solid rgba(23, 23, 23, 0.13);
		color: #6b7178;
		font-family: 'IBM Plex Mono', ui-monospace, SFMono-Regular, Menlo, monospace;
		font-size: 0.58rem;
		letter-spacing: 0.03em;
		text-transform: uppercase;
	}

	@media (max-width: 700px) {
		.portfolio-footer {
			align-items: start;
			gap: 2.5rem;
			min-height: 22rem;
		}

		.portfolio-footer__meta {
			align-items: start;
			flex-direction: column;
		}
	}
</style>
